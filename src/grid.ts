import { GridOptions } from "@logicflow/core/types/view/overlay/Grid";

export const size = 20;

export default {
    size: size,
    visible: true,
    type: "dot",
    config: {
      color: "#ababab",
      thickness: 1
    }
} as GridOptions
